#include "regular-expression-matching.hpp"

using namespace std;

bool isMatch(string s, string p)
{
    // char *sCStr = s.c_str();

    // char *pCStr = p.c_str();
    // int pIndex = 0;

    // char p0 = p[pIndex];
    // char p1 = p[pIndex + 1];

    // int startMatch = 0;
    // int endMatch = 0;

    // if ( )
    return false;
}
